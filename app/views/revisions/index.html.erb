<h4>Revisions for <%= link_to @post.title, post_path(@post) %></h4>

<% @revisions.each do |r| %>
  <div>
    <%= link_to r.created_at, post_revision_path(@post, r) %> by <%= r.post.user.email %>
  </div>
<% end %>
